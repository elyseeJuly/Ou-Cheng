# 偶成 (Ou Cheng) — 语料全量扩容与动态字体选择系统任务进度清单
> **文档类别**: 阶段执行任务清单 (`EXEC_TASK`)  
> **关联会话**: 语料大扩容（含朝代）与动态字体字库重构  
> **真实时间**: 2026-04-16  
> **设计依据**: [SPEC_20260520_GLOBAL_DEVELOPMENT_STANDARDS.md](file:///Users/quantumrose/Documents/Emberois/SPEC_20260520_GLOBAL_DEVELOPMENT_STANDARDS.md)

---

## 📅 阶段开发任务看板

- [x] **任务 1：升级与重构数据爬取清洗脚本**
  - [x] 扩展 [fetch-poetry.js](file:///Users/quantumrose/Documents/Emberois/ou-cheng/scripts/fetch-poetry.js) 的 SOURCES 数组，新增诗经、楚辞、曹操诗集、元曲及五代诗词 5 大经典语料源。
  - [x] 优化提取器（parse）逻辑，如对于诗经，拼接“章节+标题”做 title；对于曹操诗集，硬编码作者为“曹操”等。
  - [x] 建立严密的 `dynasty` 硬映射：唐代、宋代、先秦、汉末三国、元代、五代十国。
  - [x] 重构本地静态语料库 `poetry.json`，容纳合并共计 **1,941** 首古典巨著。

- [x] **任务 2：类型契约与基础常量重构**
  - [x] 更新 `types.ts` 中的 `Dynasty`、`Poem`、`UserSettings` 数据类型定义，全面注入 `fontStyle`。
  - [x] 在 `constants.ts` 中维护中文字体映射数组 `FONT_STYLES` 声明，为 `MOCK_CLASSICS` 数据补充朝代属性。

- [x] **任务 3：生僻字无损字库与外部 CDN 接入**
  - [x] 在 `index.html` 的头部（Head）引入 `fonts.loli.net` 镜像加速，并载入思源宋体等字体。
  - [x] 引入 `lxgw-wenkai-lite-webfont` 专门用以无损渲染极冷生僻字。

- [x] **任务 4：花笺纸预览页双层级字体控件开发**
  - [x] 修改 `PoemPreview.tsx`，导入并渲染字体下拉菜单。
  - [x] 绑定 CSS 样式属性，实现花笺纸预览框对选择字体的即时渲染。
  - [x] 编写 `handleSetGlobalFont` 触发逻辑，支持全局系统字体一键替换及快速刷新应用。

- [x] **任务 5：全局应用入口的劫持挂载与状态持久化**
  - [x] 重构 `index.tsx` 入口，在 ReactDOM 渲染前拦截设置，通过 DOM 样式覆写 `document.body.style.fontFamily`。
  - [x] 修改 `pages/Create.tsx` 页面，将当前选择的字体数据实时存入生成的诗歌对象 `fontStyle` 字段，实现随诗存取。
  - [x] 确保已保存的作品在「墨迹」（作品库）中展示时，能够完美还原其创作时所选择的专属字体。
